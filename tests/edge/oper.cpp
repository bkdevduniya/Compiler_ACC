// Shift operators vs templates
        // Comparison chain vs template
x << y >> z;        // Shift chain

// Assignment chains
a = b = c = 10;     // Right-associative assignment

// Mixed compound assignments
a += b -= c *= d;   // Complex assignment chain